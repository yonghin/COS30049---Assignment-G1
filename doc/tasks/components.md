# Module: Shared Frontend Components

## 目标
实现可复用前端组件，供多个页面复用并保持行为一致。

## 最小可执行任务
- [ ] 实现 `NavBar.jsx`，包含顶部导航链接、响应式菜单和当前页面高亮
- [ ] 实现 `SpamResultCard.jsx`，显示模型名称、判决、概率仪表盘与 Top features 条形图
- [ ] 实现 `MalwareResultCard.jsx`，显示单样本 SVM/DBSCAN 结果与雷达图布局
- [ ] 实现 `FileUploadZone.jsx`，支持拖拽、选择文件、类型/大小校验和错误提示
- [ ] 实现 `ExportButton.jsx`，通用 CSV 导出组件
- [ ] 实现 `FeatureInputAccordion.jsx`，按逻辑分组显示 39 个数值输入字段
- [ ] 实现 `ModelSelectToggle.jsx`，支持多选模型切换并验证最少一个选中
- [ ] 添加组件单元测试覆盖关键渲染和交互行为
